name: mikenson_thomas
description: Aplicativo do Mikenson.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.3
  shared_preferences: ^2.0.15
  sqflite: ^2.2.0+3
  path_provider: ^2.0.11
  image_picker: ^0.8.6
  lottie: ^1.1.0
  # firebase_core: "^1.14.0"
  # firebase_auth: "^3.3.13"
  # cloud_firestore: "^2.3.0"
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
   - assets/images/foto.jpeg